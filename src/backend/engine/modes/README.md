# `engine/modes/` — 主笔 skill 配置

| 文件 | 作用 | 关键符号 |
|------|------|----------|
| `author_loop_skill_prefs.py` | 读写 per-pipeline 的主笔 skill 编排配置（`config/pipelines/<id>/author_loop_skill_prefs.json`）。 | `load_skill_prefs`、`save_skill_prefs` |
