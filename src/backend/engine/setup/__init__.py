"""Set up the construction subsystem (included in the engine): per-story to generate world view/character/plot and other creative pre-lore.

Completely separate from per-chapter content production (`hooks/packages/` user plugins + main author loop); this subsystem is
The built-in function of the engine, based on the stage molecular package (cast/world/plot), reuses the REFINE "ask → produce → review" primitive,
The output is placed in data/lore and data/plot for production and consumption in downstream chapters."""
